export type BillingCycle = 'monthly' | 'yearly';
export type BillingConfig = { provider: string; mode: string; clientKey: string | null; enabled: boolean; customerKey: string | null; hasBillingKey: boolean; cardCompany?: string | null; cardNumberMasked?: string | null };
export type BillingPlan = { code: string; name: string; monthlyPrice: number; monthlyVat: number; monthlyTotal: number; yearlyPrice: number; yearlyVat: number; yearlyTotal: number; currency: string };
export type CheckoutSummary = { subscriptionStatus: string; billingCycle: BillingCycle; trialEndsAt: string | null; currentPeriodEnd?: string | null; nextBillingAt?: string | null; planName?: string; isEntitled?: boolean; plan: BillingPlan | null; referral: { code: string; status: string } | null; checkoutConfig?: BillingConfig };
export type CheckoutQuote = { valid: boolean; planCode: string; planName: string; billingCycle: BillingCycle; baseAmount: number; discountAmount: number; todayChargeAmount: number; nextChargeAmount: number; nextBillingAt: string | null; coupon: { code: string; type: string; freeMonths?: number | null; discountAmount: number; finalAmount: number; message?: string | null } | null; message?: string | null; summaryMessage: string; benefitKind: string | null };
export type Subscription = { status: string; planName: string; planCode: string; billingCycle: BillingCycle; pendingBillingCycle?: BillingCycle | null; autoRenewStatus?: 'AUTO_RENEW_ACTIVE' | 'CANCEL_SCHEDULED' | 'CANCELED' | 'INACTIVE'; cancelAt?: string | null; currentPeriodStart: string | null; currentPeriodEnd: string | null; nextBillingAt: string | null; nextChargeAmount?: number | null; hasBillingCredential?: boolean };
export type Payment = { id: number; status: string; amount: number; vatAmount: number; totalAmount: number; billingCycle: string; provider: string; planCode: string | null; planName: string; paidAt: string | null; createdAt: string };
export type ManageSummary = { summary: CheckoutSummary; subscription: Subscription | null; payments: Payment[]; referral: { code: string; status: string } | null };
