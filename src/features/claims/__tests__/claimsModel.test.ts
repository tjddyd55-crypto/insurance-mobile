import { claimMessage, claimStatusMeta, extractClaimFileUrl, formatClaimDate } from '../claimsModel';
describe('claimsModel', () => { it('maps every claim status', () => { expect(claimStatusMeta('requested')).toEqual({ label: '요청됨', tone: 'info' }); expect(claimStatusMeta('done').tone).toBe('success'); }); it('builds readable content and dates', () => { expect(claimMessage('보험금 청구', '진단서 첨부')).toBe('보험금 청구\n진단서 첨부'); expect(formatClaimDate(null)).toBe('—'); }); it('prefers signed download links', () => { expect(extractClaimFileUrl({ url: 'open', downloadUrl: 'signed' }, true)).toBe('signed'); }); });
